$(document).ready(function(){
  // $('.main-display-one').slick();
  $('.main-display').slick({
    dots: true,              // Adds navigation dots
    infinite: true,          // Loops the slides
    speed: 300,              // Slide transition speed in milliseconds
    slidesToShow: 1,         // Shows one slide at a time
    adaptiveHeight: true     // Adjusts the height based on the content
});

});

$(document).ready(function(){
  // $('.main-display-one').slick();
  $('.footer-content-text').slick({
    dots: true,              // Adds navigation dots
    infinite: true,          // Loops the slides
    speed: 300,              // Slide transition speed in milliseconds
    slidesToShow: 1,         // Shows one slide at a time
    adaptiveHeight: true     // Adjusts the height based on the content
});

});
  
  